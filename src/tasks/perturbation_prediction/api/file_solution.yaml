type: file
description: "Solution (complete data)"
example: "TODO"
info:
  label: Solution
  slots:
    layers:
      - type: integer
        name: counts
        description: Raw counts
      - type: double
        name: normalized
        description: Normalized counts
    uns:
      - type: list
        name: phenotypes
        description: "A list of phenotype names in layers"
        required: true
