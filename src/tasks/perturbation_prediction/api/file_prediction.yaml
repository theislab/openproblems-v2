type: file
description: "Prediction (method output)"
example: "TODO"
info:
  label: Prediction
  slots:
    layers:
      - type: integer
        name: counts
        description: Raw counts
      - type: double
        name: normalized
        description: Normalized counts
    uns:
      - type: list
        name: phenotypes
        description: "A list of phenotype names in layers"
        required: true
